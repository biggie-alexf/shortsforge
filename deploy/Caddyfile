# Реальный TLS через sslip.io (домен указывает на IP сервера автоматически).
# Когда появится собственный домен — заменить адрес сайта и всё.
{
	email onii.chan.sasha@gmail.com
}

178-104-185-191.sslip.io {
	handle /api/* {
		reverse_proxy api:8000
	}
	handle /media/* {
		reverse_proxy api:8000
	}
	handle {
		reverse_proxy frontend:80
	}
}
